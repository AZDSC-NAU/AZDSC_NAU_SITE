Test site for Quarto.

To run, clone the repo, and download quarto and uv. If you need to compile R scripts download R and the package renv. 

If everything is working correctly `uv run quarto preview` should open the website locally. 
